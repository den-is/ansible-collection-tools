- name: Tap hashicorp brew repository
  community.general.homebrew_tap:
    name: hashicorp/tap
    state: present

- name: Brew install yazi
  community.general.homebrew:
    name: '{{ yazi_brew_package }}'
    state: '{{ yazi_brew_state }}'
    update_homebrew: true
