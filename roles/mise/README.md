# mise - dev tools, env vars, task runner
Environments and tools version management for various tools and programming platforms

## Links
- https://github.com/jdx/mise
- https://mise.jdx.dev/

## Deploying man pages
By default man base dir is `/usr/share/man` and requires privileged permissions to deploy in that directory.  
Set it to `~/.local/share/man` and make sure this directory exists. Including `man1` `man5` and other subdirs.
